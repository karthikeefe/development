// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyCd_Vclq94xVdVOYsL1RFVRcZJP4YVbkD8",
    authDomain: "project1-fb055.firebaseapp.com",
    databaseURL: "https://project1-fb055.firebaseio.com",
    projectId: "project1-fb055",
    storageBucket: "",
    messagingSenderId: "593722288553",
    appId: "1:593722288553:web:bf8343f290d9c528"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
